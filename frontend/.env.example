# 浏览器请求 API 的前缀。开发阶段留空即可：走同源 http://localhost:3000/api/*，
# 由 next.config.ts 的 rewrites 转发到 FastAPI。
NEXT_PUBLIC_API_BASE_URL=

# 仅影响 next build / next dev 时写入的 rewrite 目标；默认 http://127.0.0.1:8000
# BACKEND_URL=http://127.0.0.1:8000
